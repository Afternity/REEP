<Window x:Class="REEP.WPF_Client.Frontend.Views.UserViews.UpdateUserWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:REEP.WPF_Client.Frontend.Views.UserViews"
        mc:Ignorable="d"
        Title="UpdateUserWindowView" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="0,5,0,2"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="5"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="MinWidth" Value="100"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FF2D2D30" Padding="10">
            <Grid>
                <TextBlock Text="Создание нового пользователя" 
                Foreground="White"
                FontSize="16"
                VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Выход" 
             Command="{Binding CloseUpdateUserWindowCommand}"
             HorizontalAlignment="Right"
             Padding="10 5" Margin="5"
             Style="{StaticResource ModernButtonStyle}"/>
                </StackPanel>

            </Grid>
        </Border>


        <!-- Детали пользователя -->
        <ScrollViewer Grid.Row="1" Grid.RowSpan="5" Grid.Column="4" Grid.ColumnSpan="2"
            VerticalScrollBarVisibility="Auto"
            Margin="10">
            <StackPanel>
                <StackPanel DataContext="{Binding SelectedUser}">
                    <TextBlock Text="Детальный конструктор:" FontWeight="Bold" Margin="0,0,0,10"/>


                    <Separator/>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Имя:"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding FirstName}"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Фамилия:"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SecondName}"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Отчество:"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding LastName}"/>

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Email:"/>
                    <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Email}"/>

                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Контакты:"/>
                    <TextBox Grid.Row="5" Grid.Column="1" 
                            Text="{Binding OtherContacts}"
                            AcceptsReturn="True"
                            Height="60"
                            TextWrapping="Wrap"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Тип:"/>
                    <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Type}"/>
                    <Separator/>

                </StackPanel>
                <Button Content="Сохранить"
                        Command="{Binding UpdateUserCommand}"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
